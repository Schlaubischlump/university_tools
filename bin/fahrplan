#!/usr/bin/env python3

import sys

from universitytools.timetable import print_connections

if __name__ == "__main__":
    if len(sys.argv) != 2:
        print("Only one argument, a station name is required!", file=sys.stderr)
    else:
        print_connections(sys.argv[1])
